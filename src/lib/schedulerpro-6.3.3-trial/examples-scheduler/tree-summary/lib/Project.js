import SchedulerResourceModel from '../../../lib/Scheduler/model/ResourceModel.js';

export default class Project extends SchedulerResourceModel {
    static get fields() {
        return [
        ];
    }
}
